services:
  cyber-graph-triage:
    build: .
    ports:
      - "8080:8080"
    environment:
      NEO4J_URI: ${NEO4J_URI:-bolt://neo4j:7687}
      NEO4J_USER: ${NEO4J_USER:-neo4j}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD:-change-me}
      NEO4J_DATABASE: ${NEO4J_DATABASE:-neo4j}
      NEO4J_WS_BRIDGE_TARGET: ${NEO4J_WS_BRIDGE_TARGET:-}
      NEO4J_WS_BRIDGE_LISTEN_HOST: ${NEO4J_WS_BRIDGE_LISTEN_HOST:-127.0.0.1}
      NEO4J_WS_BRIDGE_CONNECT_HOST: ${NEO4J_WS_BRIDGE_CONNECT_HOST:-127.0.0.1}
      NEO4J_WS_BRIDGE_LISTEN_PORT: ${NEO4J_WS_BRIDGE_LISTEN_PORT:-17687}
      NEO4J_WS_BRIDGE_ENABLED: ${NEO4J_WS_BRIDGE_ENABLED:-true}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
    restart: unless-stopped
